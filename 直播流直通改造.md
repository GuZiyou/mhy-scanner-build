# 直播流取帧改造（对齐 2 号版本 KFC.v50 的"快"）

> 目标：把 2 号版本在**直播流扫码**上比上游快的原因，按源码级方式补进本工程。
>
> 本文件说明改了什么、为什么、以及编译/运行时要确认的事情。

---

## 一、上游（原版）慢在哪

证据来自三个 exe 的导入表转储（`_analysis\str_f1/f2/f3.txt`）与上游源码对照：

| 环节 | 上游 0 号源码（= 1 号 exe 同架构） | 2 号（KFC.v50） |
| --- | --- | --- |
| 解码 | `WeChatQRCode::detectAndDecode`：`cvtColor` 灰度 → SSD 检测网络@0.4× → 每个候选框再做超分网络 + 多档 zxing 复解 | `libzbar64-0.dll` 的 `zbar_scan_image` 一次 |
| 取帧 | 软解，导入 `sws_scale` / `swscale-9.dll`：每帧全画幅转 BGR24（720p ≈ 2.7MB 写入 + 分配） | 导入 `av_hwdevice_ctx_create`、`av_hwframe_transfer_data`、`avcodec_default_get_format`（硬解指纹），**完全没有 swscale** |
| 图像处理 | 全彩 BGR | 只有 `cv::resize` / `cv::threshold` + `cv::Mat(rows,cols,type,data,step)`（把 Y 平面按 stride 包成灰度图） |

换算下来，上游每帧是「软解 + 全画幅 BGR 转换 + 两级 CNN」，2 号每帧是「硬解 + 取 Y 平面 + 二值化 + ZBar」。

解码器那部分由本包的 `src\Core\QRScanner.{h,cpp}` 负责（ZBar + 预处理阶梯，已实现）。
**本文件负责的是剩下那一半：取帧链路。**

---

## 二、本包改了什么

只改了 `src\UI\QRCodeForStream.{h,cpp}` 两个文件（`apply.ps1` 会覆盖进工程），
`QRCodeForScreen`（屏幕扫描，DXGI 出的是 BGRA）与业务逻辑一行未动。

### 1. D3D11VA 硬件解码，失败自动回落

`init()` 中在 `avcodec_open2` 之前：

```cpp
av_hwdevice_ctx_create(&pHwDeviceCtx, AV_HWDEVICE_TYPE_D3D11VA, nullptr, nullptr, 0)
pAVCodecContext->hw_device_ctx = av_buffer_ref(pHwDeviceCtx);
pAVCodecContext->get_format  = getHwPixelFormat;   // 找不到 D3D11 就 avcodec_default_get_format
```

- 设备创建失败（无显卡 / 远程桌面 / 驱动不支持）→ **完全不设置** `get_format`，软解路径与上游一致；
- 运行日志会打印实际生效的方式：

  ```
  [QRCodeForStream] 取帧方式: D3D11VA 硬解 + Y 平面直通（灰度）
  ```

### 2. 灰度直通：不再经过 sws_scale 的 BGR24

`buildScanImage()` 的优先级：

1. **硬解帧** → `av_hwframe_transfer_data` 回拷到 CPU 的复用帧（`pAVFrameSW`）；
2. 取 `data[0]` + `linesize[0]` 作为 **Y 平面**，用 `cv::Mat(h, w, CV_8UC1, data, linesize).clone()` 得到连续灰度图；
3. 硬解回拷失败 / 非平面 YUV / 10bit（P010 等）→ 回落到上游的 `sws_scale` → BGR24 路径。

`clone()` 的必要性有两处：去掉 `linesize` 的行尾对齐填充（ZBar 要求连续缓冲），以及让异步解码任务持有自己的内存，不受 FFmpeg 复用帧缓冲影响。代价是 **1 字节/像素**（720p ≈ 0.9MB），相比原来的 BGR24（3 字节/像素 + 色彩变换）仍然明显更省。

`QRScanner` 侧**不需要改动**：`scanWithLadder()` 已经按 `frame.channels()` 分支，`CV_8UC1` 直接进 ZBar，屏幕路径的 4 通道 BGRA 也照旧。

### 3. 预览窗口

`cv::imshow` 现在收到的是单通道灰度图，OpenCV 会按灰度显示，`#ifndef SHOW` 的开关行为与上游一致（注意上游这个宏是反的：**不**定义 `SHOW` 才会显示窗口）。

### 4. 送进解码器的是全分辨率灰度帧

灰度直通后 `QRScanner` 收到的是原始分辨率的灰度帧（上游是缩到 720p 再转 BGR），
所以解码侧的"每帧成本"变成了新的瓶颈：阶梯越长、图越大，无码帧就越贵。

这一点已经在 `扫码加速清单.md` 里一并处理：直播流默认走 `ScanMode::Fast`
（短阶梯 + 扫描宽度上限 1280），不再跑 OpenCV / WeChat 兜底，也关掉了 ZBar
那个"每次调用都复制整图反色重扫"的 `TEST_INVERTED`。
解码侧的完整改动与调参旋钮见那份文档。

---

## 三、编译与运行注意

- 不需要新增任何依赖：`av_hwdevice_ctx_create` / `av_hwframe_transfer_data` 都在 `avutil` 里，工程本来就链接 FFmpeg 全套（见 `cmake/install_ffmpeg.cmake`）。
- 新增头文件：`libavutil/hwcontext.h`、`libavutil/pixdesc.h`（都包在原有的 `extern "C"` 块里）。
- FFmpeg 用 CI 那套 BtbN `gpl-shared` 构建即可，Windows 版自带 d3d11va。
- 若想强制关掉硬解做对比：把 `init()` 里那段 `av_hwdevice_ctx_create` 注释掉即可（或改成每次都 `hwDecodeRequested = false`）。

## 四、怎么确认它真的在起作用

1. 启动后看控制台那行 `[QRCodeForStream] 取帧方式:`；
2. 硬解生效时，任务管理器里 FFmpeg 相关线程的 CPU 占用应明显低于软解；
3. 用同一段直播流、同一台机器，对比改造前后「二维码出现在画面上 → 认定 ticket」的时间；
4. 单帧解码耗时可以用本包的 `scan_test`（灰度图直接喂 `decodeSingle`）横向对比 ZBar 与 WeChatQRCode。

## 五、风险与未验证声明

1. **本改造没有在本机编译/运行验证过** —— 本机没有 VS / Qt / vcpkg / cmake（见 `_analysis\env.txt`）。代码按 FFmpeg 稳定公开 API 编写，关键签名已对照上游头文件核对（`av_hwdevice_ctx_create`、`av_hwframe_transfer_data`、`AV_PIX_FMT_FLAG_HWACCEL/RGB`、`av_pix_fmt_desc_get`）。
2. 硬解在部分机器上可能**更慢**（GPU 回拷带宽差、或码率很低的 1080p 软解本来就只要几毫秒）。真正的稳定收益来自第 2 条（去掉 BGR 转换）与 ZBar 解码；如果实测硬解反而变慢，按第三节关掉即可。
3. 2 号还有一处**未确认**的做法：它导入了 `cv::Mat(const Mat&, const Rect&)`（子区域构造），可能对中心区域做过裁剪后再扫码。本包没有跟这条（`QRScanner` 的第 6 级阶梯已经覆盖"中心裁剪放大"的效果），因为无法从导入表确认。
