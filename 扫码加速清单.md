# 扫码加速清单（本次一起改掉的）

> 本文汇总"让扫码更快"的全部改动：每一项都写明**原理 → 预计影响 → 怎么回退**。
> 取帧链路（硬解 + 灰度直通）的细节在 `直播流直通改造.md`，本文不重复。
>
> 结论先行：真正的大头有三个——
> ① 取帧不再走 swscale/BGR；② 直播流只跑 ZBar 短阶梯、不再每帧跑 WeChatQRCode 的两级 CNN；
> ③ 不再让 ZBar 在每个阶梯里都"复制整图 + 反色重扫"。

---

## 一、总览

| # | 改动 | 原理 | 预计影响 | 回退方式 |
| --- | --- | --- | --- | --- |
| 1 | 直播流**硬解 + 灰度直通**（D3D11VA + Y 平面） | 省掉每帧全画幅 BGR 转换与 2~3MB 分配；解码交给 GPU | 每帧固定开销大幅下降 | `直播流直通改造.md` 第三节 |
| 2 | **引擎分级 `ScanMode`**；直播流用 `Fast` | `Fast` 完全不跑 OpenCV / WeChat 兜底 | **最大单项**：不再每帧跑 WeChat 的检测 CNN + 超分 CNN + zxing 复解 | 改回 `Normal` / `Full` |
| 3 | **关掉 ZBar 的 `TEST_INVERTED`**，反色改由阶梯自己做 | ZBar 的该开关是**每次 `zbar_scan_image()` 调用**级别的：没找到码就 `_zbar_image_copy()` 复制整图反色后再全图重扫 | 一帧"没码"从"6 次复制 + 12 次全图扫描"降到"2 次反色 + 8 次扫描"（Fast 档更低） | 恢复 `initZBar()` 里那一行配置 |
| 4 | 直播流**短阶梯**（4~6 次 ZBar，全在 ≤1.3MP 图上） | 去掉 CLAHE、去掉"放大图上做自适应阈值"；全图放大只对本来就小的画面做 | 单帧从几十~上百毫秒降到个位数~十几毫秒 | 用 `Normal` 模式即回到完整阶梯 |
| 5 | 大图不再做**全图放大**，改为**中心 3/5 裁剪 ×2** | 放大不会增加信息；1080p 全图放大到 2880p 纯属浪费，而抢码时二维码基本居中 | 放大类的开销降到 1/3 以下 | `centerZoom()` 的 3/5、2.0 参数 |
| 6 | 自适应阈值 `GAUSSIAN_C` → `MEAN_C` | 块大小 41 的高斯版本在百万像素图上很贵；均值版本走盒滤波/积分图 | 该级快约一个数量级（仅完整阶梯用到） | `ladderFull()` 里改回 `ADAPTIVE_THRESH_GAUSSIAN_C` |
| 7 | 放大插值 `INTER_CUBIC` → `INTER_LINEAR` | 送给 ZBar 的图后面都要二值化，线性插值足够 | 放大本身快 2~3 倍 | `ladderFast/ladderFull()` 里改回 `INTER_CUBIC` |
| 8 | 直播流扫描宽度上限 **1920 → 1280**（`FAST_MAX_SCAN_WIDTH`） | ZBar 开销与像素数成正比 | 该级像素数降到 ~44% | 改这个常量 |
| 9 | `Fast` 档顺便保证：命中的帧尽早返回 | 阶梯严格按"便宜优先"排序 | 平均延迟下降 | — |

> 第 3 条值得单独强调：原来的写法在小码/水印多、**大多数帧都扫不到**的直播场景里，
> 等于把每一级阶梯的代价都乘了 2（还各带一次整图内存拷贝）。这是隐藏得最深的一处浪费。

---

## 二、三档扫描强度

`QRScanner::ScanMode`（`src/Core/QRScanner.h`）：

| 模式 | 阶梯 | 兜底引擎 | 用在哪 | 单帧量级 |
| --- | --- | --- | --- | --- |
| `Fast` | 短阶梯：原图 / 反色 / Otsu / 中心放大（+小图全局放大） | 无 | **直播流（已默认）** | 几毫秒 ~ 十几毫秒 |
| `Normal`（默认） | 完整阶梯：原图 / Otsu / 反色 / 放大 / 自适应 / CLAHE / 中心放大 | `cv::QRCodeDetector` | 屏幕扫描、图片自测 | 十几 ~ 几十毫秒 |
| `Full` | 同 `Normal` | 再加 `WeChatQRCode`（两级 CNN） | 只在确实需要时 | 可能上百毫秒 |

调用点：

```cpp
// 直播流（src/UI/QRCodeForStream.cpp，已在补丁里设好）
thread_local QRScanner qrScanners;
qrScanners.setMode(QRScanner::ScanMode::Fast);

// 屏幕扫描（src/UI/QRCodeForScreen.cpp，未改动 —— 用默认的 Normal）
```

**默认值的变化请注意**：`Normal` 不再包含 `WeChatQRCode`。上游是把 WeChat 当唯一引擎用的，
现在它退回到"可选兜底"。如果哪台机器上出现"二维码明明在屏幕上却扫不到"，
第一件事就是把模式改成 `Full` 复测（一行）：

```cpp
qrScanners.setMode(QRScanner::ScanMode::Full);
```

---

## 三、怎么验证（不需要抓流、不需要登录）

`tools/scan_test.cpp` 加了三个参数，可以直接量化每一档的差别。

> **不用自己编译**：`SCAN_TEST` 现在默认打开（`CMakeLists.txt` 里 `option(SCAN_TEST ... ON)`），
> 并且补上了之前漏掉的 `install(TARGETS scan_test ...)`，所以 CI / 发布产物里就已经带着
> `scan_test.exe`（和 ZBar 的 DLL 在同一目录）。把下面的 `build\Release\` 换成你解压产物后的目录即可。

```powershell
# 彩色读图 + 完整阶梯（相当于屏幕扫描）
build\Release\scan_test.exe --mode normal --repeat 20 doc\image\hk4e_qrcode_1080.png

# 灰度读图 + 直播流档（相当于硬解 Y 平面直通的输入）
build\Release\scan_test.exe --mode fast --gray --repeat 20 doc\image\hk4e_qrcode_1080.png

# 三档横比（注意 full 需要 ./ScanModel，且会明显慢）
build\Release\scan_test.exe --mode full --repeat 5 doc\image\*.png
```

输出会给出**单帧最小 / 平均耗时**、命中引擎和载荷内容。
建议再用一张**没有二维码**的图（例如随便截一张桌面 `doc\image` 之外的 png）各跑一遍——
"扫不到"的帧才是直播场景里的常态，也最能体现第 2、3、4 条改动的价值。

> 用默认的 `DEV=OFF` 构建即可：CMakeLists 会定义 `TESTSPEED` 和 `SHOW`，
> 从而**关掉 `decodeSingle()` 里的每帧日志和 `cv::imshow` 预览窗口**，计时才干净。
> 反过来，用 `-DDEV=ON` 构建会打开这两样，程序会明显更慢——排查"为什么我这版慢"时先看这个。

---

## 四、还想更快时的旋钮（按性价比排序）

1. **ZBar 扫描线密度**：`initZBar()` 里
   `ZBAR_CFG_X_DENSITY` / `ZBAR_CFG_Y_DENSITY` 目前是 `1`（每一行、每一列都扫，且每个方向正反各扫一遍 = 4 遍全图）。
   改成 `2` 可再砍掉约一半扫描线，代价是小二维码的召回率。
2. **`FAST_MAX_SCAN_WIDTH`**：1280 → 960。直播间的登录码通常仍然够清晰。
3. **扫描帧率上限**：现在每个解码出来的帧都会送扫（`tryStart` 在池忙时直接丢帧）。
   如果直播是 60fps，可以每隔一帧送一次——延迟最多多 16ms，CPU 直接减半。
4. **`threadNumber`**：`QRCodeForStream.h` 里是 `2`。`Fast` 档下单帧只要几毫秒，
   2 个线程足够覆盖 60fps；真不够再加。
5. **OpenCV 内部线程**：`cv::resize/threshold` 会走 OpenCV 的全局线程池，
   和我们的 2 个解码线程叠加可能过订阅，可在 `main()` 里 `cv::setNumThreads(1)` 试试。

---

## 五、取舍与未验证声明

1. **`Fast` 档放弃了兜底引擎**：ZBar 短阶梯认不出的码，`Fast` 不会再去用 OpenCV / WeChat 找。
   这是"抢码要快"的取舍；`Normal`/`Full` 保留完整能力，随时可切。
2. **`Normal` 默认不再跑 WeChat**：功能上等于把上游的默认引擎降级为可选项（见第二节）。
3. 第 6、7 条（`MEAN_C`、`INTER_LINEAR`）属于"近似等价"的替换，理论上可能让极少数
   低质量图片的召回率略降；若发现某类图扫不到，先按表中"回退方式"逐个换回去定位。
4. **本次改动仍未经编译与运行验证**：本机没有 VS / Qt / vcpkg / cmake（`_analysis\env.txt`）。
   所有 API 用法（ZBar C API、OpenCV、FFmpeg hwaccel）都按上游源码与公开头文件核对过，
   但首次编译仍可能需要微调。
5. 本清单里的耗时"量级"是依据算法复杂度（像素数、调用次数）推断的相对量级，
   **不是**本机实测值；请用第三节的 `scan_test` 在你自己的机器上量。
