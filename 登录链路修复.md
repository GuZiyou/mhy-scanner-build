# 登录链路现状与证据（务必先读这一份）

> 本文记录 2026-09 这一轮排查的**实测证据**。之前我写过一份"旧 combo/panda 接口已失效"
> 的说明，那份的结论是错的，已作废；本文取代它。

## 一、三个版本到底谁是谁（我一开始搞错了）

| 目录 | 实际身份 | 扫码引擎 | 登录是否可用 |
| --- | --- | --- | --- |
| `0\MHY_Scanner-main` | 上游 **旧版 v1.1.15** 源码（我们所有构建都基于它） | — | **不可用**（就是"加不上账号"的那版） |
| `1\` | 别人编译好的 **v1.16.0**（PDB 里的源码路径是 `D:\CodeRepos\.DSVVA\MHY_Scanner`） | ZXing | **可用** |
| `2\1\` | KFC.v50（另一个分支） | ZBar | 旧登录，不可用 |

**我犯的错误**：以为 `0\` 就是"最新版源码"，于是把登录链路"恢复原版"——其实是恢复到了
那个加不上账号的旧版本，所以出现了"又添加不上账号了"。已改回 passport 方案。

判断依据（可复现）：直接扫两个 exe 的字符串 —
- `1\MHY_Scanner.exe` 含 `app/createQRLogin`、`app/queryQRLoginStatus`、`app/scanQRLogin`、
  `app/confirmQRLogin`、`app/getCookieAccountInfoBySToken`，**不含** `combo/panda/qrcode/fetch`、
  `getTokenByGameToken`、`getGameToken`。
- 我们基于 `0\` 构建出来的 exe 恰好相反。

## 二、能用那版 1.16.0 的真实实现（从反编译 + 字符串还原）

添加账号：
```
POST passport-api.mihoyo.com/account/ma-cn-passport/app/createQRLogin
     headers: Content-Type: application/json, x-rpc-app_id: dw9y09jqjpxc, x-rpc-device_id: <uuid4>
     body: {}
  -> data.url（二维码内容）, data.ticket（36 位）

POST .../app/queryQRLoginStatus   body: {"ticket":"<ticket>"}
  -> status=Confirmed 时 data.tokens[0].token 就是 stoken(v2)，data.user_info 给出 aid/mid/account_name
```
扫描登录（两个模板函数 `QRCodeLogin<0>` = scan、`QRCodeLogin<1>` = confirm）：
```
① POST api-sdk.mihoyo.com/{biz}/combo/panda/qrcode/scan          （= PandaScanQRCode）
   body: {"app_id":<gameType>,"device":<uuid4>,"ticket":"<24位>","passport_app_id":"bll8iq97cem8","ts":<unix>}
   -> data.passport_qr_url = https://user.mihoyo.com/login-platform/mobile.html?expire=..&tk=<uuid>&token_types=1#/login/qr

② POST passport-api.mihoyo.com/account/ma-cn-passport/app/scanQRLogin
   headers: x-rpc-app_id: bll8iq97cem8, x-rpc-device_id: <uuid4>, Cookie: stoken=<stoken>;mid=<mid>
   body: {"ticket":"<url 里 tk= 到 & 之间>","token_types":"<url 里 token_types= 到 # 之间>"}

③ POST .../app/confirmQRLogin   同上 body/headers
```
- 反编译里 URL 是硬编码的，`tk` 截到 `&`、`token_types` 截到 `#` —— 与我们的 `ParseQrTicket` 一致。
- **反编译里没有** `createAuthTicketByGameBiz`、`ma-cn-verifier`、`getGameToken`、panda `confirm`。
  也就是说之前那两次尝试（payload 带 stoken / payload 带 authTicket 的 panda confirm）都不是 1.16.0 的做法。

## 三、当前卡点（已做完的实验，全部失败）

用 `1\Config\userinfo.json` 里**现成的、已验证有效的**账号
（`getCookieAccountInfoBySToken` 返回 retcode 0）实测 `app/scanQRLogin`：

| 变量 | 试过的值 | 结果 |
| --- | --- | --- |
| 请求头组合 | 只有 3 头 / 加完整 GetRequestHeader / 无 Cookie / 无 device / 换 UA（curl、miHoYoBBS、okhttp） | 全 `-502` |
| Content-Type | 不设 / `text/plain`(cpr 默认) / `application/json`(±charset) / `x-www-form-urlencoded` | 全 `-502` |
| `x-rpc-app_id` | `bll8iq97cem8` / `dw9y09jqjpxc` | 全 `-502` |
| `x-rpc-device_id` | 随机 uuid4 / exe 内三个 UUID 常量 / 不传 | 全 `-502` |
| `token_types` | `"1"`(字符串) / `1`(数字) / `"0"` | 全 `-502` |
| ticket 来源 | panda 返回的 `passport_qr_url` 里的 tk / `createQRLogin` 的 ticket | 全 `-502` |
| Cookie 字段 | `stoken=` / `stoken_v2=` / 追加 `cookie_token=` | 全 `-502` |
| 方法 | GET | `MethodNotAllowed`（说明必须 POST） |
| 网络路径 | 走 Clash 系统代理 / 显式直连 | 全 `-502` |
| 参数不合法对照 | 只发 `{"ticket":...}` 不带头 | `-3001 参数不合法`（说明接口是通的、参数会被校验） |

也就是说：**接口是活的、参数被校验，但凡是"齐全"的请求一律 -502**，
而能用那版显然能拿到 retcode 0。差异就在我还没能观测到的地方。

其它顺带证实的事实：
- `GET api-takumi…/auth/api/getGameToken?stoken=&mid=` 对**有效** stoken 也返回
  `{"retcode":-1,"message":"decode err response body error: unexpected end of JSON input"}`
  —— 这个接口对我们不可用（能用那版也不用它）。
- `GET api-takumi…/binding/api/getUserGameRolesByStoken` 返回 `-10001 invalid request`
  —— 拿不到各游戏 `game_uid`（B 方案走不通）。
- `POST passport…/ma-cn-verifier/app/createAuthTicketByGameBiz` 对有效 stoken 返回 retcode 0，
  且 `uid` 传游戏 uid 或 mid **都返回 0** —— 说明它不校验 uid，游戏端拒绝登录的原因不在这里。
- `panda scan` 是**真实生效**的：扫过之后 `panda query` 从 `stat:"Init"` 变成 `retcode:-3503`。

## 四、下一步（唯一还有效的手段）

既然把请求"照抄"到这一步仍然 -502，唯一可靠的办法是**看能用那版真正发出去了什么**：
把 `1\MHY_Scanner.exe` 跑在本地 HTTPS 中间人代理后面（给它装一个临时自签 CA、
设 `HTTPS_PROXY` 环境变量——libcurl 会认这个变量），抓下 `scanQRLogin`/`confirmQRLogin`
的完整明文请求。拿到之后照着改，一次就能对。

在那之前，**能正常用的是 `1\MHY_Scanner.exe` 本身**（只是没有我们做的扫码加速）。

## 五、其它已经交付且与本问题无关的改动（都保留）

- Fast/Normal/Full 三档扫码、ZBar 动态加载、DHD11VA 硬解 + Y 平面直通、
  预处理阶梯优化、`scan_test.exe` —— 见 `扫码加速清单.md` 与 `直播流直通改造.md`。
- 添加账号用 passport `/app/` 接口（可用）；启动校验用 `getCookieAccountInfoBySToken`（可用）。
